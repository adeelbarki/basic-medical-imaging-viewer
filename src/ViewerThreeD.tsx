import { init as coreInit } from '@cornerstonejs/core';
import { init as dicomImageLoaderInit } from '@cornerstonejs/dicom-image-loader';

export default function ViewerThreeD() {
    return(
        <div>Hello from Viewr3D</div>
    )
}